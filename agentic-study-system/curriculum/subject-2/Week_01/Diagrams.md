# 知识图解

## 知识结构图
```mermaid
flowchart TD
  A["第1周：无线信息论安全入门"] --> B["示例导入：Alice、Bob、Eve"]
  A --> C["核心知识：无线安全为何重要"]
  A --> D["利用衰落生成秘密密钥"]
  A --> E["安全通信流程"]

  B --> B1["Alice 发送消息给 Bob"]
  B --> B2["Eve 在旁边窃听"]
  B --> B3["无线信号会广播到空气中"]

  C --> C1["信道随机波动带来不确定性"]
  C --> C2["传统密码学依赖高层算法"]
  C --> C3["物理层安全利用信道本身的随机性"]

  D --> D1["主信道与窃听者信道强弱会变化"]
  D --> D2["只在 Bob 更有利时刻传输随机符号"]
  D --> D3["共享随机性可整理成秘密密钥"]

  E --> E1["随机性共享"]
  E --> E2["消息协调"]
  E --> E3["隐私放大"]
  E --> E4["用密钥加密真正消息"]

  C1 --> F["保密容量"]
  D1 --> F
  F --> F1["Bob 更强时保密容量为正"]
  F --> F2["Eve 更强时保密容量为 0"]
  F --> F3["衡量信道可安全传多少"]

  E2 --> G["多级编码"]
  E2 --> H["LDPC 码"]
  E3 --> I["压缩掉 Eve 已知信息"]

  D --> J["结论：衰落既是挑战，也是安全资源"]
  F --> J
  E --> J
```

## 图解说明
- 无线通信的天然特性是“广播到空气中”，因此 Eve 可能被动接收，这也是无线安全问题重要的根本原因。
- 课程的核心思想不是单纯依赖传统密码算法，而是利用信道的随机衰落，把“对 Bob 更有利的时刻”转化为安全机会。
- 保密容量刻画了信道在某次衰落下能否安全传输：Bob 强于 Eve 时才有正的安全速率，否则为 0。
- 机会式秘密密钥协商的主线是“先共享随机性，再协调一致，再隐私放大，最后用密钥保护消息”。
- 多级编码和 LDPC 码主要用于消息协调阶段，帮助 Bob 从带噪观测中恢复与 Alice 一致的随机序列。
- 总体结论是：衰落不只是噪声来源，也可以成为生成秘密密钥和实现物理层安全的资源。
